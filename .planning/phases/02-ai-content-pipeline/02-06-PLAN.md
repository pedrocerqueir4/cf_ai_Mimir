---
phase: 02-ai-content-pipeline
plan: 06
type: execute
wave: 4
depends_on: ["02-03", "02-04"]
files_modified:
  - apps/web/app/routes/_app.roadmaps.$id.lessons.$lessonId.tsx
  - apps/web/app/routes/_app.roadmaps.$id.quiz.tsx
  - apps/web/app/components/lesson/QuizQuestion.tsx
  - apps/web/app/components/lesson/LessonContent.tsx
autonomous: true
requirements:
  - CONT-03
  - CONT-04
  - UX-02
must_haves:
  truths:
    - "User can read bite-sized lesson content rendered as Markdown"
    - "User can answer quiz questions at the bottom of each lesson"
    - "Quiz feedback is instant: correct shows accent border, wrong shows destructive border + correct answer revealed"
    - "User can complete a lesson after answering all questions"
    - "Practice quiz mode presents randomized questions from completed lessons"
  artifacts:
    - path: "apps/web/app/routes/_app.roadmaps.$id.lessons.$lessonId.tsx"
      provides: "Lesson view with reading content and inline quiz"
      contains: "Knowledge Check"
    - path: "apps/web/app/routes/_app.roadmaps.$id.quiz.tsx"
      provides: "Practice quiz mode for knowledge reinforcement"
      contains: "Practice Quiz"
    - path: "apps/web/app/components/lesson/QuizQuestion.tsx"
      provides: "Reusable quiz question component with instant feedback"
      contains: "Correct"
  key_links:
    - from: "apps/web/app/routes/_app.roadmaps.$id.lessons.$lessonId.tsx"
      to: "/api/roadmaps/:id/lessons/:lessonId"
      via: "TanStack Query useQuery"
      pattern: "fetchLesson"
    - from: "apps/web/app/components/lesson/QuizQuestion.tsx"
      to: "/api/quiz/:questionId/answer"
      via: "fetch POST on option tap"
      pattern: "submitQuizAnswer"
---

<objective>
Build the lesson view with Markdown content rendering, inline quiz with instant feedback, lesson completion flow, and standalone practice quiz mode.

Purpose: This is where learning happens. Users read bite-sized content (CONT-03) with comprehension quizzes (CONT-04) and get immediate feedback (UX-02). Practice quiz enables spaced repetition (D-13).
Output: Lesson view page, practice quiz page, reusable quiz question and lesson content components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ai-content-pipeline/02-CONTEXT.md
@.planning/phases/02-ai-content-pipeline/02-UI-SPEC.md
@.planning/phases/02-ai-content-pipeline/02-03-SUMMARY.md
@.planning/phases/02-ai-content-pipeline/02-04-SUMMARY.md

<interfaces>
<!-- From Plan 04 — API client functions -->
From apps/web/app/lib/api-client.ts:
```typescript
export async function fetchLesson(roadmapId: string, lessonId: string): Promise<LessonDetail>;
export async function submitQuizAnswer(questionId: string, selectedOptionId: string): Promise<QuizAnswerResult>;
export async function completeLesson(roadmapId: string, lessonId: string): Promise<void>;
export async function fetchPracticeQuiz(roadmapId: string): Promise<QuizQuestion[]>;

interface LessonDetail {
  id: string; title: string; content: string; // Markdown
  quiz: { id: string; questions: QuizQuestion[] };
}
interface QuizQuestion {
  id: string; questionText: string; questionType: "mcq" | "true_false";
  options: { id: string; text: string }[];
  order: number;
}
interface QuizAnswerResult {
  correct: boolean; correctOptionId: string; explanation: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build quiz question component and lesson content renderer</name>
  <files>
    apps/web/app/components/lesson/QuizQuestion.tsx,
    apps/web/app/components/lesson/LessonContent.tsx
  </files>
  <read_first>
    apps/web/app/lib/api-client.ts,
    .planning/phases/02-ai-content-pipeline/02-UI-SPEC.md
  </read_first>
  <action>
    **1. Create `apps/web/app/components/lesson/LessonContent.tsx`** — renders Markdown lesson content:

    - Use `react-markdown` (installed in Plan 01) to render AI-generated Markdown safely (no dangerouslySetInnerHTML)
    - Body text: 16px / 400 / 1.5 line-height
    - Markdown heading mapping: `h2` → 20px/600, `h3` → 16px/600, `p` → 16px/400
    - Max content width: `max-w-[680px] mx-auto` on desktop, full-width `px-6` on mobile (24px padding)
    - Apply Tailwind prose-like styles via className on ReactMarkdown components prop:
      - `h2`: `text-xl font-semibold mt-6 mb-3`
      - `h3`: `text-base font-semibold mt-4 mb-2`
      - `p`: `text-base leading-relaxed mb-4`
      - `ul`/`ol`: `list-disc pl-6 mb-4` / `list-decimal pl-6 mb-4`
      - `code`: `bg-card px-1 py-0.5 rounded text-sm`
      - `pre`: `bg-card p-4 rounded-lg overflow-x-auto mb-4`

    Props: `{ content: string }` (Markdown string)

    **2. Create `apps/web/app/components/lesson/QuizQuestion.tsx`** — reusable quiz question with instant feedback per D-11, D-12, UX-02, UI-SPEC Screen 4:

    Props:
    ```typescript
    interface QuizQuestionProps {
      question: QuizQuestion;
      onAnswered: (correct: boolean) => void;
    }
    ```

    **Rendering:**
    - Card container with question text at top (Body, 16px, 400)
    - Options stacked vertically, each option is a full-width row:
      - MCQ (4 options): radio-style leading indicator (circle outline), label text (14px, 400), `min-h-[52px]` per option (UI-SPEC exception)
      - True/False: 2 stacked rows, same pattern
    - `role="radiogroup"` on the options container per UI-SPEC accessibility
    - Each option has `role="radio"` and `aria-checked`
    - All options have `min-h-[52px]` to accommodate stacking without crowding

    **Interaction (D-12 instant feedback):**
    - Tap any option → auto-submit (no confirm step)
    - Call `submitQuizAnswer(question.id, selectedOptionId)` immediately
    - Once tapped, ALL options become non-interactive (`pointer-events-none`)
    - Feedback renders within 150ms (local state update + API response)

    **Feedback display (per UI-SPEC):**
    - **Correct:** Selected option gets `border-primary` (accent border), `CheckCircle` icon replaces radio indicator
    - **Wrong:** Selected option gets `border-destructive`, `XCircle` icon; correct option simultaneously shows `border-primary` (accent)
    - Explanation text appears below the options: Label (14px), muted-foreground
    - After feedback, the `role="alert"` container announces correct/wrong to screen readers
    - Feedback text: "Correct" or "Incorrect" per UI-SPEC copywriting

    **No green color** — correct uses accent (blue), wrong uses destructive (red) per UI-SPEC color section.
  </action>
  <verify>
    <automated>cd /home/pedro/Documents/cf_ai_Mimir && grep -q "react-markdown" apps/web/app/components/lesson/LessonContent.tsx && grep -q "role=\"radiogroup\"" apps/web/app/components/lesson/QuizQuestion.tsx && grep -q "CheckCircle" apps/web/app/components/lesson/QuizQuestion.tsx && grep -q "XCircle" apps/web/app/components/lesson/QuizQuestion.tsx && grep -q 'role="alert"' apps/web/app/components/lesson/QuizQuestion.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "react-markdown" apps/web/app/components/lesson/LessonContent.tsx
    - grep -q "max-w-\[680px\]" apps/web/app/components/lesson/LessonContent.tsx
    - grep -q 'role="radiogroup"' apps/web/app/components/lesson/QuizQuestion.tsx
    - grep -q 'role="radio"' apps/web/app/components/lesson/QuizQuestion.tsx
    - grep -q "CheckCircle" apps/web/app/components/lesson/QuizQuestion.tsx
    - grep -q "XCircle" apps/web/app/components/lesson/QuizQuestion.tsx
    - grep -q "border-primary" apps/web/app/components/lesson/QuizQuestion.tsx
    - grep -q "border-destructive" apps/web/app/components/lesson/QuizQuestion.tsx
    - grep -q 'role="alert"' apps/web/app/components/lesson/QuizQuestion.tsx
    - grep -q "pointer-events-none" apps/web/app/components/lesson/QuizQuestion.tsx
    - grep -q "submitQuizAnswer" apps/web/app/components/lesson/QuizQuestion.tsx
    - grep -q "Correct" apps/web/app/components/lesson/QuizQuestion.tsx
    - grep -q "Incorrect" apps/web/app/components/lesson/QuizQuestion.tsx
  </acceptance_criteria>
  <done>LessonContent renders Markdown safely via react-markdown. QuizQuestion provides instant feedback with accent/destructive borders, screen reader announcements, and 52px option height.</done>
</task>

<task type="auto">
  <name>Task 2: Build lesson view page and practice quiz page</name>
  <files>
    apps/web/app/routes/_app.roadmaps.$id.lessons.$lessonId.tsx,
    apps/web/app/routes/_app.roadmaps.$id.quiz.tsx
  </files>
  <read_first>
    apps/web/app/components/lesson/QuizQuestion.tsx,
    apps/web/app/components/lesson/LessonContent.tsx,
    apps/web/app/lib/api-client.ts,
    .planning/phases/02-ai-content-pipeline/02-UI-SPEC.md
  </read_first>
  <action>
    **1. Create `apps/web/app/routes/_app.roadmaps.$id.lessons.$lessonId.tsx`** — Lesson view per D-10, UI-SPEC Screen 4:

    **Layout:**
    - Page header: Back button (`ChevronLeft` + roadmap title truncated to 24 chars + ellipsis), lesson title (Heading, 20px, semibold)
    - Full-width scrollable content: `LessonContent` at top, quiz section at bottom
    - Fixed footer bar with "Ask AI" button (for in-lesson Q&A — built in Plan 07, but the button must be present)

    **Data fetching:**
    - Use TanStack Query: `useQuery({ queryKey: ["lesson", roadmapId, lessonId], queryFn: () => fetchLesson(roadmapId, lessonId) })`
    - Loading state: `Skeleton` blocks (3 lines of varying width)
    - Error: "This lesson doesn't exist." per UI-SPEC

    **Quiz section (D-10 — content + quiz on same page):**
    - Divider: shadcn `Separator` with centered label "Knowledge Check" (per UI-SPEC copywriting)
    - Render `QuizQuestion` for each question in quiz.questions array
    - Track which questions have been answered (local state)
    - Between questions: show "Next question" button (shadcn `Button` variant default, full-width) per UI-SPEC
    - On last question: show "Finish lesson" button instead of "Next question" per UI-SPEC

    **Lesson completion:**
    - After all quiz questions answered: show "Complete lesson" button (shadcn `Button` variant default, full-width) per UI-SPEC
    - On tap: call `completeLesson(roadmapId, lessonId)`, then navigate back to `/roadmaps/${roadmapId}`
    - Invalidate roadmap detail query to refresh node completion states

    **Fixed footer with "Ask AI" (D-17):**
    - Fixed at bottom (above bottom nav on mobile): `fixed bottom-16 lg:bottom-0`
    - shadcn `Button` with icon `MessageCircle` (Lucide), label "Ask AI"
    - This button will open the in-lesson Q&A bottom sheet — Plan 07 will wire the sheet; for now the button should exist but show a toast "Coming soon" on click

    **2. Create `apps/web/app/routes/_app.roadmaps.$id.quiz.tsx`** — Practice quiz per D-13, UI-SPEC Screen 5:

    **Layout:**
    - Page header: Back button + "Practice Quiz"
    - Heading: "Quiz: {Roadmap Title}" (Heading, 20px, semibold)
    - Progress indicator: "Question {N} of {M}" — Label (14px), muted-foreground

    **Data fetching:**
    - Use TanStack Query: `useQuery({ queryKey: ["practice-quiz", roadmapId], queryFn: () => fetchPracticeQuiz(roadmapId) })`

    **Quiz interaction:**
    - Same `QuizQuestion` component reuse
    - One question at a time, centered on screen (full-width on mobile)
    - Same instant feedback as lesson quiz
    - No reading content — quiz only (D-13)

    **Completion:**
    - After last question: summary card showing score "{X} of {M} correct"
    - Two CTAs: "Try again" (resets quiz with new random order) and "Back to roadmap" (navigates to `/roadmaps/${roadmapId}`)

    **Empty state (per UI-SPEC):**
    - "Complete at least one lesson before taking a practice quiz." (Body, 16px, muted-foreground)
    - CTA: "Go to roadmap" → `/roadmaps/${roadmapId}`
  </action>
  <verify>
    <automated>cd /home/pedro/Documents/cf_ai_Mimir && grep -q "Knowledge Check" apps/web/app/routes/_app.roadmaps.\$id.lessons.\$lessonId.tsx && grep -q "Complete lesson" apps/web/app/routes/_app.roadmaps.\$id.lessons.\$lessonId.tsx && grep -q "Ask AI" apps/web/app/routes/_app.roadmaps.\$id.lessons.\$lessonId.tsx && grep -q "Practice Quiz\|Quiz:" apps/web/app/routes/_app.roadmaps.\$id.quiz.tsx && grep -q "Try again" apps/web/app/routes/_app.roadmaps.\$id.quiz.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Knowledge Check" apps/web/app/routes/_app.roadmaps.$id.lessons.$lessonId.tsx
    - grep -q "LessonContent" apps/web/app/routes/_app.roadmaps.$id.lessons.$lessonId.tsx
    - grep -q "QuizQuestion" apps/web/app/routes/_app.roadmaps.$id.lessons.$lessonId.tsx
    - grep -q "Complete lesson" apps/web/app/routes/_app.roadmaps.$id.lessons.$lessonId.tsx
    - grep -q "Next question" apps/web/app/routes/_app.roadmaps.$id.lessons.$lessonId.tsx
    - grep -q "Finish lesson" apps/web/app/routes/_app.roadmaps.$id.lessons.$lessonId.tsx
    - grep -q "Ask AI" apps/web/app/routes/_app.roadmaps.$id.lessons.$lessonId.tsx
    - grep -q "fetchLesson" apps/web/app/routes/_app.roadmaps.$id.lessons.$lessonId.tsx
    - grep -q "fetchPracticeQuiz" apps/web/app/routes/_app.roadmaps.$id.quiz.tsx
    - grep -q "Try again" apps/web/app/routes/_app.roadmaps.$id.quiz.tsx
    - grep -q "Back to roadmap" apps/web/app/routes/_app.roadmaps.$id.quiz.tsx
    - grep -q "Complete at least one lesson" apps/web/app/routes/_app.roadmaps.$id.quiz.tsx
  </acceptance_criteria>
  <done>Lesson view shows reading content + quiz with instant feedback and completion flow. Practice quiz provides standalone reinforcement mode with score summary. "Ask AI" button is present (sheet wired in Plan 07).</done>
</task>

</tasks>

<verification>
- Lesson page renders Markdown content safely (no raw HTML)
- Quiz questions show 4 MCQ options or 2 T/F options
- Instant feedback on tap — correct=accent border, wrong=destructive border
- Answer key (correctOptionId) only appears AFTER submission
- "Complete lesson" marks lesson done and refreshes node tree
- Practice quiz shows score and allows retry
- All copy matches UI-SPEC copywriting contract
</verification>

<success_criteria>
- Lesson content renders AI-generated Markdown via react-markdown
- Quiz provides instant per-question feedback per D-12, UX-02
- Lesson completion flow works end-to-end
- Practice quiz mode works with score tracking per D-13
- All accessibility attributes present (radiogroup, alert, aria-checked)
- All loading/empty/error states match UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-content-pipeline/02-06-SUMMARY.md`
</output>
