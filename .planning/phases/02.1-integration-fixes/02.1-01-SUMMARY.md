---
phase: 02.1-integration-fixes
plan: 01
subsystem: security-middleware
tags: [rate-limiting, sanitize, middleware, security, wrangler-bindings]
dependency_graph:
  requires: []
  provides: [rate-limiting-on-auth, global-sanitize-middleware, rate-limiter-bindings]
  affects: [apps/web/workers/app.ts, apps/web/wrangler.jsonc, apps/web/worker-configuration.d.ts]
tech_stack:
  added: []
  patterns: [hono-middleware-chain, cloudflare-rate-limiting]
key_files:
  created: []
  modified:
    - apps/web/workers/app.ts
    - apps/web/wrangler.jsonc
    - apps/web/worker-configuration.d.ts
decisions:
  - Applied both authRateLimit and registerRateLimit on all /api/auth/* paths (no sub-path differentiation) per plan D-01
  - Added RATE_LIMITER_AUTH and RATE_LIMITER_REGISTER to local AppEnv interface for type consistency
  - Regenerated worker-configuration.d.ts via wrangler types to pick up new bindings
metrics:
  duration: 278s
  completed: 2026-04-16T13:59:17Z
  tasks_completed: 2
  tasks_total: 2
  files_modified: 3
---

# Phase 02.1 Plan 01: Wire Rate Limiting and Sanitize Middleware Summary

Rate limiting (authRateLimit + registerRateLimit) and global sanitize middleware wired into apps/web/workers/app.ts with rate limiter bindings declared in wrangler.jsonc.

## What Was Done

### Task 1: Add rate limiter bindings and wire security middleware
**Commit:** `101d6f6`

- Added `unsafe.bindings` to `apps/web/wrangler.jsonc` with `RATE_LIMITER_AUTH` (10 req/min, namespace 1001) and `RATE_LIMITER_REGISTER` (5 req/min, namespace 1002), matching `worker/wrangler.toml`
- Added imports for `authRateLimit`, `registerRateLimit` from `worker/src/middleware/rate-limit` and `sanitize` from `worker/src/middleware/sanitize`
- Mounted `api.use("/api/*", sanitize)` globally after CORS, before all route handlers
- Mounted `api.use("/api/auth/*", authRateLimit, registerRateLimit)` before the Better Auth handler

### Task 2: Verify TypeScript compilation
**Commit:** `bcc0d36`

- Added `RATE_LIMITER_AUTH: RateLimit` and `RATE_LIMITER_REGISTER: RateLimit` to the `AppEnv` interface in `app.ts`
- Regenerated `worker-configuration.d.ts` via `wrangler types` to include rate limiter binding types in the global `Env`
- Verified no new TypeScript errors introduced by the middleware changes (all TS6307 errors are pre-existing from the cross-package import pattern used by all worker imports)

## Middleware Chain Order

```
CORS -> sanitize (all /api/*) -> authRateLimit + registerRateLimit (/api/auth/*) -> route handlers
```

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added rate limiter types to AppEnv interface**
- **Found during:** Task 2
- **Issue:** The `AppEnv` interface in `app.ts` (used as Hono Bindings type) did not include `RATE_LIMITER_AUTH` or `RATE_LIMITER_REGISTER`, which would cause a type mismatch with the middleware expecting these on the context bindings
- **Fix:** Added both rate limiter fields to `AppEnv` and regenerated `worker-configuration.d.ts`
- **Files modified:** `apps/web/workers/app.ts`, `apps/web/worker-configuration.d.ts`
- **Commit:** `bcc0d36`

## Pre-existing Issues Noted

- `tsconfig.cloudflare.json` extends a non-existent `tsconfig.json` (TS5083)
- All worker cross-package imports produce TS6307 errors (files not in project include pattern) -- this affects all existing imports (schema, routes, workflows), not just the new middleware imports
- `react-router typegen` fails due to missing entry.server.tsx resolution
- Various `node_modules` type conflicts (better-auth, zod, @better-fetch)

## Self-Check: PASSED
